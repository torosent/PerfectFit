namespace PerfectFit.UseCases;

/// <summary>
/// Placeholder class for the UseCases (Application) layer.
/// This layer contains application logic, commands, queries, and handlers.
/// It depends only on the Core layer.
/// </summary>
public static class UseCasesAssemblyMarker
{
    public const string AssemblyName = "PerfectFit.UseCases";
}
