namespace PerfectFit.Infrastructure;

/// <summary>
/// Placeholder class for the Infrastructure (Data Access) layer.
/// This layer contains database context, repositories, and external service implementations.
/// It depends on the Core layer.
/// </summary>
public static class InfrastructureAssemblyMarker
{
    public const string AssemblyName = "PerfectFit.Infrastructure";
}
